from math import *

x = float(input("Пожалуйста, введите x: "))

a = ceil(sin(x**2))
b = x**4
count = 0

while a <= b:
    if a % 2 == 0:
        count += 1
    a += 1

print("В этом промежутке ", count, " чётных чисел")
