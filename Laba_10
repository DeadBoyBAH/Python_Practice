from math import *

x = float(input("Введите x: "))

s = 0

for k in range(1, 5):
    s1 = 0
    for n in range(1, 10):
        if k >= n:
            a = 0.1
        else:
            a = 1

        s1 += pow(a*x, k/n)

    s += s1

print("Сумма = ", s)
